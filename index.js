// Challenge 1

// 1

function sum() {
  let numbers = [1, 2, 3, 4, 999];
  let b = Number(numbers.slice(-1));
  a = numbers[0];

  return (sum = a + b);
}
console.log(sum());

// 2

function alwaysFive() {
  var temp = "AMAAAAZING";
  Z = (temp.match(/A/g) || []).length;
  return Z;
}
console.log(alwaysFive());
